// How many sitemap files to publish. Increase these slowly over time.
// Each movie/TV file covers 20 TMDB pages, roughly 400 titles.
export const MOVIE_SITEMAPS = 2;
export const TV_SITEMAPS = 1;
export const PAGES_PER_SITEMAP = 20;
